#pragma once
// yet another "we need to do this or there'll be cyclic dep errors" meme

#include "cglm/types.h"

// a Position on the grid, meaningful for logic comparisons/collisions
typedef ivec2 Position;
